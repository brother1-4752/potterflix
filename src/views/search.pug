extends base.pug
include mixins/video

block content
    form(method="GET").search__form
        input(placeholder="Search by title",name="keyword", type="text")
        button
            i.fas.fa-search
    div.video-grid
        each video in videos 
            +video(video)
        else
            span.empty__message 키워드를 입력하세요